import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center"
  },
  headerTitile:{
    fontWeight:'bold',
    fontSize:20,
    padding:5,
    color:'#000000'
  }
});

export default styles;
